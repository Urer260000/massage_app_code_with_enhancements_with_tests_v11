name: flutter_automated_tests

environment:
  sdk: '>=3.0.0-0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_goldens:
    sdk: flutter
  integration_test:
    sdk: flutter
  platform: 3.1.0
  test: 1.24.3
  # ... (other autogenerated dependencies)

dev_dependencies:
  flutter_driver:
    sdk: flutter
  # ... (other dev_dependencies, if any)

flutter:
  uses-material-design: true
  assets:
    - icon/test.png

# PUBSPEC CHECKSUM: 8050